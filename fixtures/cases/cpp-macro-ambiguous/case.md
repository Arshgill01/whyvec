# C++ macro-origin loop ambiguity

Two macro expansions introduce loops from one invocation line. The optimization
query must retain an identity ambiguity decline rather than selecting one
expanded loop by proximity.
