# Benchmark log

No performance result is accepted until correctness validation passes and raw samples are retained.

Each entry records the analyzed commit, compiler and target fingerprint, CPU and frequency policy, workload and input digest, warmup policy, sample count, raw artifact path, summary statistics, dispersion, comparison method, and reasons any samples were rejected.
